import argparse

parser = argparse.ArgumentParser(
    description = 'Diagnose RAG System. Performs comprehensive health checks on all components, validates configurations, and reports issues to ensure optimal system operation',
    add_help = False,
    formatter_class = argparse.ArgumentDefaultsHelpFormatter,
)
